<div class="card">
  <p><%= @article.title %></p>
  <p><%= @article.content %></p>
</div>
<%= link_to "edit", edit_article_path(@article), :class => 'btn btn-primary m-2' %>
<%= link_to "Delete",
  article_path(@article),
  :class => 'btn btn-primary m-2',
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<%= link_to "Home", articles_path, :class => 'btn btn-primary m-2' %>
